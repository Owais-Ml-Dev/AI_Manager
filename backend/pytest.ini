[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
markers =
    live_ai: live end-to-end tests that call real AI providers and a running Flask server
    no_db: pure unit tests that do not require MongoDB initialization
